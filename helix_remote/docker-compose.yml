services:
  helix-backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    container_name: helix-backend
    ports:
      - "127.0.0.1:8080:8080"
    volumes:
      - helix-data:/app/data
    env_file:
      - .env
    restart: unless-stopped

volumes:
  helix-data:
