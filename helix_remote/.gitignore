# Dart/Flutter
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
# pubspec.lock is committed on purpose - see
# docs/dependencies/WORKSPACE_LOCKFILE_POLICY.md. This is an application
# workspace, not a published library, so the lockfile is the reproducible
# dependency snapshot and CI enforces it with `pub get --enforce-lockfile`.
build/
*.iml
.idea/
.vscode/
coverage/

# Firebase deployment material. Ties the build to a specific Firebase
# project and is supplied per deployment, not committed. Its absence is a
# supported state: app/build.gradle.kts skips the Google Services plugin and
# FirebasePushTokenSource reports push unavailable.
**/google-services.json
**/GoogleService-Info.plist

# Android
app/android/.gradle/
app/android/local.properties
app/android/GeneratedPluginRegistrant.java
app/android/key.properties
*.keystore
*.jks

# Flutter generated build config (per-platform, per-app)
**/ephemeral/

# Backend
backend/build/
backend/bin/*.exe

# Runtime data
attachments_storage/
backup_media_storage/
*.db
*.db-shm
*.db-wal

# OS
.DS_Store
Thumbs.db

# Deployment secrets - copy .env.example to .env and fill in real values
.env
