name: helix_remote_crypto
description: Cryptographic primitives for Helix Remote, including X3DH, Double Ratchet, Sender Keys, and secure storage.
publish_to: none
version: 1.0.0

environment:
  sdk: ^3.12.0

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  meta: ^1.18.0
  cryptography: ^2.9.0
  cryptography_flutter: ^2.0.0
  crypto: ^3.0.7
  flutter_secure_storage: ^10.3.1
  helix_remote_domain:
    path: ../helix_remote_domain

dev_dependencies:
  flutter_test:
    sdk: flutter
