name: helix_remote_tool
description: Extraction and tooling scripts for helix_remote.
publish_to: none
version: 1.0.0

environment:
  sdk: ^3.12.0

resolution: workspace

dependencies:
  helix_remote_domain:
    path: ../packages/helix_remote_domain

dev_dependencies:
  test: ^1.31.0
