name: helix_tool
description: Architecture boundary checker and tooling scripts.
publish_to: none
version: 1.0.0

environment:
  sdk: ^3.12.0

resolution: workspace

dependencies:
  yaml: ^3.1.3
  crypto: ^3.0.7

dev_dependencies:
  test: ^1.31.0
