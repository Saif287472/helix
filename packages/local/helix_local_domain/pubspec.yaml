name: helix_local_domain
description: Domain entities, core constants, repository contracts, and state machines.
publish_to: none
version: 1.0.0

environment:
  sdk: ^3.12.0

resolution: workspace

dependencies:
  meta: ^1.18.0
  crypto: ^3.0.7

dev_dependencies:
  test: ^1.31.0
