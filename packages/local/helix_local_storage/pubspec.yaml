name: helix_local_storage
description: SQLite database, secure storage, and in-memory repository implementations.
publish_to: none
version: 1.0.0

environment:
  sdk: ^3.12.0

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  basic_utils: ^5.8.2
  convert: ^3.1.2
  crypto: ^3.0.7
  flutter_secure_storage: ^10.3.1
  path: ^1.9.1
  path_provider: ^2.1.6
  sqlite3: ^3.3.3
  sqlite3_flutter_libs: ^0.6.0
  helix_local_domain:
    path: ../helix_local_domain

dev_dependencies:
  flutter_test:
    sdk: flutter
