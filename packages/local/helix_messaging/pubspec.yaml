name: helix_messaging
description: Clean Messaging Use Cases and Adapters for Helix
publish_to: none
version: 1.0.0

environment:
  sdk: ^3.12.0

resolution: workspace

dependencies:
  uuid: any
  helix_domain:
    path: ../helix_domain
  helix_protocol:
    path: ../helix_protocol
  helix_transport:
    path: ../helix_transport
