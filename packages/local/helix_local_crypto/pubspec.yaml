name: helix_local_crypto
description: Session key derivation, HKDF, and chain-key ratcheting.
publish_to: none
version: 1.0.0

environment:
  sdk: ^3.12.0

resolution: workspace

dependencies:
  cryptography: any
  crypto: any
  helix_local_domain:
    path: ../helix_local_domain
