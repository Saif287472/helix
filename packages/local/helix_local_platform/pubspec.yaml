name: helix_local_platform
description: Android foreground service, Windows tray, and platform notification/diagnostics gateways.
publish_to: none
version: 1.0.0

environment:
  sdk: ^3.12.0

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  connectivity_plus: ^7.1.1
  flutter_local_notifications: ^22.0.1
  network_info_plus: ^8.1.0
  tray_manager: ^0.5.3
  helix_local_domain:
    path: ../helix_local_domain
  helix_local_protocol:
    path: ../helix_local_protocol
