name: helix_local_transport
description: TCP/TLS SecureChannel, length-prefixed frame I/O, and connection lifecycle.
publish_to: none
version: 1.0.0

environment:
  sdk: ^3.12.0

resolution: workspace

dependencies:
  basic_utils: any
  cryptography: any
  pointycastle: any
  crypto: any
  meta: any
  helix_local_domain:
    path: ../helix_local_domain
  helix_local_protocol:
    path: ../helix_local_protocol
  helix_local_crypto:
    path: ../helix_local_crypto
