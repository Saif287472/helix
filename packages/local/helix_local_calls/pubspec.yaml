name: helix_local_calls
description: WebRTC call state, call service state machine, and call signaling infrastructure.
publish_to: none
version: 1.0.0

environment:
  sdk: ^3.12.0

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  flutter_webrtc: any
  uuid: any
  helix_local_domain:
    path: ../helix_local_domain
  helix_local_protocol:
    path: ../helix_local_protocol
  helix_local_transport:
    path: ../helix_local_transport
