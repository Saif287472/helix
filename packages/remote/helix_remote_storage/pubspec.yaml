name: helix_remote_storage
description: Encrypted local SQLite persistence layer for Helix Remote.
publish_to: none
version: 1.0.0

environment:
  sdk: ^3.12.0

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  sqlite3: ^3.3.3
  sqlite3_flutter_libs: ^0.6.0
  meta: ^1.18.0
  path: ^1.9.1
  helix_remote_domain:
    path: ../helix_remote_domain

dev_dependencies:
  flutter_test:
    sdk: flutter
