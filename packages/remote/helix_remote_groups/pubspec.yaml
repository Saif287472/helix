name: helix_remote_groups
description: Remote group lifecycle, membership, and administration for Helix Remote. Isolated from Local LAN group protocols.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.0

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  crypto: ^3.0.7
  helix_remote_domain:
    path: ../helix_remote_domain
  helix_remote_storage:
    path: ../helix_remote_storage

dev_dependencies:
  flutter_test:
    sdk: flutter
  path: ^1.9.1
