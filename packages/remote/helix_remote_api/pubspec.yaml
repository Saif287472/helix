name: helix_remote_api
description: HTTP REST client definitions and WebSocket envelope parser for Helix Remote.
publish_to: none
version: 1.0.0

environment:
  sdk: ^3.12.0

resolution: workspace

dependencies:
  meta: ^1.18.0
  helix_remote_domain:
    path: ../helix_remote_domain

dev_dependencies:
  test: ^1.31.0
