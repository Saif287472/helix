name: helix_tool
description: Architecture boundary checker and tooling scripts.
publish_to: none
version: 1.0.0

environment:
  sdk: ^3.12.0

resolution: workspace

dependencies:
  helix_local_domain:
    path: ../packages/local/helix_local_domain
  helix_local_protocol:
    path: ../packages/local/helix_local_protocol
  yaml: ^3.1.3

dev_dependencies:
  test: ^1.31.0
